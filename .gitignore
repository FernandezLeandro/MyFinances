# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variables de entorno — acá viven las claves de Supabase
.env
.env.*
!.env.example
# Dummy, sin ninguna clave real — hace falta commiteado para que `supabase.ts` no explote en tiempo
# de módulo si algún test futuro importa (aunque sea transitivamente) algo que llegue ahí.
!.env.test

# Coverage de Vitest
coverage/

# Metadata local del CLI de Supabase (link, db push) — se regenera sola en cada máquina
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
